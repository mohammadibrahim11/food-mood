import React from 'react';
import Header from '../Header/Header';

const Menu = () => {
    return (
        <div>
            <Header></Header>
            <h1>this is menu page</h1>
        </div>
    );
};

export default Menu;